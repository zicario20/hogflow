# Phase 10.3B Detector Generalization Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Preserve the immutable V1 failure evidence, diagnose the consumed B source, and produce a provisional V2 development artifact without claiming independent generalization before a new C source exists.

**Architecture:** Reuse HogFlow's existing annotation, YOLO training, evaluation, and Phase 10.2 runtime boundaries. Add only the manifest policy needed to represent temporal blocks from multiple development clips; keep the original single-clip temporal policy and Phase 10.3A catalog unchanged. Store diagnostics, datasets, overlays, and model weights locally under ignored roots.

**Tech Stack:** Python 3.12, pytest, Ruff, OpenCV, Ultralytics 8.4.135, existing HogFlow YOLO trainer and detection evaluator.

**Spec:** User-provided Phase 10.3B — Detector V1 Error Analysis & Generalization Improvement.

## Global Constraints

- V1 artifact and original A/B metrics remain immutable and reproducible.
- `demo_video_b` is consumed development evidence for V2 and is never called an independent V2 holdout.
- No training occurs before data-integrity and bounded visual/metric diagnostics.
- Only class ID 0 / class name `pig` is permitted.
- No media, labels, overlays, weights, checkpoints, absolute paths, or private sidecars are committed.
- V2 remains `DEMO MODEL — NOT PRODUCTION VALIDATED` and requires a new unseen `demo_video_c` for an independent claim.

### Task 1: Add multi-source temporal manifest policy

**Files:**
- Modify: `src/hogflow/annotation/models.py`
- Modify: `src/hogflow/annotation/manifest.py`
- Modify: `src/hogflow/annotation/validation.py`
- Test: `tests/test_phase_10_3b_manifest_policy.py`

- [x] Write a failing test proving a temporal-blocked manifest can explicitly contain separate non-overlapping blocks for two clips while the existing single-clip policy still rejects multi-clip manifests.
- [x] Run the focused test and observe the expected failure.
- [x] Add the smallest enum/validation branch for a multi-source temporal development policy; preserve all existing source-isolated and single-source temporal checks.
- [x] Run focused and regression tests.

### Task 2: Materialize and validate the V2 A+B development dataset

**Files:**
- Create locally ignored: `data/annotations/phase10_3b_v2/`
- Create locally ignored: `data/evaluation/phase10_3b/`
- Validation: local ignored manifest and annotation-validation reports; the
  source-only split-policy contract is covered by
  `tests/test_phase_10_3b_manifest_policy.py`.

- [x] Reuse the 100 human-reviewed A/B frames without pseudo-labeling.
- [x] Assign A train/validation blocks and B train/calibration blocks with positive temporal buffers; write an explicit sanitized manifest and fingerprints.
- [x] Validate image/label pairs, class mapping, split isolation, temporal coverage, and duplicate-frame absence before training.
- [x] Confirm B is not represented as an independent holdout in V2 metadata.

### Task 3: Preserve V1 diagnostics and train bounded V2 experiments

**Files:**
- Create sanitized doc: `docs/phase_10/phase_10_3b_detector_generalization.md`
- Update: `HOGFLOW_PROJECT_MEMORY.md`

- [x] Retain the local diagnostic JSON/overlays and write a bounded Markdown root-cause summary.
- [x] Train at most the justified small experiment matrix with the existing YOLO trainer, fixed seed, moderate augmentation, and CPU/GPU provenance.
- [x] Select V2 only from A+B development/calibration evidence; record config and artifact fingerprints.
- [x] Keep exactly one active model in the Phase 10.3 discovery root and retain V1 under an ignored historical root if needed.

### Task 4: Integrate, verify, and stop at the honest evidence gate

**Files:**
- Add tests only if a source change is required.

- [x] Load V2 through `PigDetectorConfiguration → Ultralytics adapter → ByteTrack → crossing → counter → HMI`.
- [x] Measure runtime and internal metrics; do not tune against C because C is absent.
- [ ] Run full quality gates, inspect privacy and Git hygiene, commit only source/tests/sanitized docs, push, and verify CI.
- [x] Report status `PARTIALLY COMPLETE — NEW INDEPENDENT HOLDOUT REQUIRED` unless an explicitly authorized and human-annotated C is available.
