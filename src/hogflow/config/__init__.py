"""Explicit immutable configuration foundations for HogFlow.

Current status: FOUNDATION ONLY — introduced in Phase 2.1.
"""

from hogflow.config.settings import LoggingSettings, RuntimeSettings

__all__ = ["LoggingSettings", "RuntimeSettings"]
